# A generic, single database configuration.

[alembic]
# path to migration scripts.
script_location = %(here)s/alembic

# sys.path path, will be prepended to sys.path if present.
prepend_sys_path = .

# Use os.pathsep. Default configuration used for new projects.
path_separator = os

# the output encoding used when revision files
# are written from script.py.mako
output_encoding = utf-8

# database URL. This is now set in your env.py to read from the .env file.
# The line below is no longer the primary source but is kept as a fallback.
sqlalchemy.url = postgresql://mtomas:ctTWeEBkKw0PA2vumgxme9Wn5m8ULBVZ@dpg-d0pirije5dus73dshsu0-a.frankfurt-postgres.render.com/prod_cost_calc_db


[post_write_hooks]
# ... (rest of the file is okay) ...

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S