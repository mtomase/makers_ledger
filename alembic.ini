# alembic.ini

# A generic, single database configuration.

[alembic]
# path to migration scripts.
script_location = %(here)s/alembic

# sys.path path, will be prepended to sys.path if present.
prepend_sys_path = .

# Use os.pathsep. Default configuration used for new projects.
path_separator = os

# the output encoding used when revision files
# are written from script.py.mako
output_encoding = utf-8

# database URL. This is a fallback. The primary source should be your models.py
# or alembic/env.py which reads from your .env file.
#sqlalchemy.url = postgresql://mtomas:ctTWeEBkKw0PA2vumgxme9Wn5m8ULBVZ@dpg-d0pirije5dus73dshsu0-a.frankfurt-postgres.render.com/prod_cost_calc_db
sqlalchemy.url = mssql+pyodbc://user:password@server/database?driver=ODBC+Driver+18+for+SQL+Server

# ... (rest of the file is unchanged) ...